name: changelog_cli
description: Tag-to-tag changelog generator for the groceries monorepo apps.
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.6.0
  nocterm: ^0.9.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
